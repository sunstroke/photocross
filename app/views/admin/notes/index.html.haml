%h1 АДМИНИСТРИРОВАНИЕ НОВОСТЕЙ
%p= link_to "СОЗДАТЬ НОВОСТЬ", new_note_path
    
    
%table{ :border => "1", :style => "width: 80%" }
  %caption
  %tbody
    %tr
      %td{ :width => "60%" }
        %h3 НОВОСТИ
      %td
        %h3 СОБЫТИЕ, К КОТОРОМУ ПРИВЯЗАНА
      %td
        %h3 УРЛ
      %td
        %h3 ДАТА        
      %td{ :width => "20%" }
        %h3 ОПЕРАЦИИ
    - @notes.each do |note|
      %tr
        %td= note.title
        %td= note.event ? link_to(note.event.title, note.event) : ""
        %td= h note.url
        %td= note.date && Russian::strftime(note.date,"%d %B %Y")        
        %td= link_to "редактировать", edit_note_path(note)
        %td= button_to "удалить", note, :method => :delete, :confirm => "Действительно удалить?"
